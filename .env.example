# Operator-approved upstream JSON endpoint. Do not commit real credentials.
MM_AUTHORIZED_UPSTREAM_URL=
# Optional. Keep this in GitHub Actions Secrets or a local environment variable.
MM_UPSTREAM_BEARER_TOKEN=
